spring:
  application:
    name: gateway
  profiles:
    active: dev

  cloud:
    nacos:
      server-addr: alya.ecs:18848
      config:
        file-extension: yml
        shared-configs:
          - data-id: common-config.yml
            group: DEFAULT_GROUP
            refresh: true
